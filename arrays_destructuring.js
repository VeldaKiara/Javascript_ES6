let [firstName, middleName, lastName] = ['Velda', 'Karimi', 'Kiara'];
//above is an array of objects. it prints  the names one after the other

lastName = 'Kent';

console.log(firstName, middleName, lastName)
//output is  Velda Karimi Kent because the last name changed.
