let example1 = [1,2,3,4,5,6];
let example2 = [...example1]; // this code unpacks all the elements in example 1 to 2 and prints them
example2.push(true); // this code adds true  to the code in example 1

console.log(example2);
//Spread operator: allows iterables( arrays / objects / strings ) to be expanded into single arguments/elements.